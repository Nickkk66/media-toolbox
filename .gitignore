node_modules/
build/
vendor/bin/*.exe
vendor/gs/*.exe
vendor/gs/*.dll
vendor/qpdf/*.exe
vendor/qpdf/*.dll
vendor/exiftool/
vendor/realesrgan/
vendor/whisper/
vendor/piper/
*.log
# Bundled demo media (copyrighted; kept local only)
src/songs/

# Promo video projects + the dropped songs (copyrighted audio; kept local only)
videos/
while im gone/

# Private working docs + the old banner image — kept local, not published
HANDOFF.md
VIDEO-PLAN.md
assets/banner.svg
